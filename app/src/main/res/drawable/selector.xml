<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    //if this icon is selected, then the icon will turn this color
    <item
        android:state_selected="true"
        android:color="?attr/accentSelect"/>

    //if this icon is not selected, then the icon will turn this color
    <item
        android:state_selected="false"
        android:color="?attr/accentUnselect"/>

</selector>

